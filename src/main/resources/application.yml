spring:
  application:
    name: students_server_registry
server:
  port: 8761

eureka:
    client:
        register-with-eureka: false
        fetch-registry: false
#    server:
#        enable-self-preservation: false
    instance:
        prefer-ip-address: true
        hostname: localhost
#    service-url:
#        defaultZone: http://localhost:8761/eureka/